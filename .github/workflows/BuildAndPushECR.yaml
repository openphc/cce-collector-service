name: Java CI and GHCR Push

on:
  push:
    branches: [ "devops/uhis-958" ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE_URI: ghcr.io/${{ github.repository }}
  IMAGE_TAG: ${{ github.sha }}
  OCI_SOURCE: https://github.com/${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - uses: actions/checkout@v5

    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    # Configure Gradle for optimal use in GitHub Actions, including caching of downloaded dependencies.
    # See: https://github.com/gradle/actions/blob/main/setup-gradle/README.md
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: Build with Gradle
      run: ./gradlew build

    - name: Build container image
      run: |
        ./gradlew bootBuildImage \
          --imageName=${IMAGE_URI}:latest \
          --environment BP_OCI_SOURCE=${OCI_SOURCE}

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.repository_owner }}
        password: ${{ secrets.GHCR_TOKEN || secrets.GITHUB_TOKEN }}

    - name: Tag and push image
      run: |
        docker tag ${IMAGE_URI}:latest ${IMAGE_URI}:${IMAGE_TAG}
        docker push ${IMAGE_URI}:${IMAGE_TAG}
        docker push ${IMAGE_URI}:latest

  # trigger-deploy: uncomment when ready to auto-deploy after push
  # trigger-deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/release-1.0.0' && success() # only for push to main and docker image is successfully created
  #   env:
  #     GH_TOKEN: ${{ secrets.CROSS_REPO_TOKEN }}
  #   steps:
  #     - name: Trigger deploy-scripts workflow_dispatch
  #       run: |
  #         gh api --method POST \
  #           /repos/openphc/deploy-scripts/actions/workflows/deploy.yml/dispatches \
  #           --input - <<'JSON'
  #         {
  #           "ref": "main",
  #           "inputs": { "target_env": "dev" }
  #         }
  #         JSON
